---
phase: 08-response-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/agent/prompts.py
  - backend/src/agent/validator.py
  - backend/src/agent/config.py
  - backend/tests/test_agent.py
autonomous: true

must_haves:
  truths:
    - "SPECIALIST_PROMPT contains explicit response format template with Direct Answer, Source Attribution, and Cross-References sections"
    - "SPECIALIST_PROMPT instructs to ALWAYS end with a Source: line from source_documents"
    - "SPECIALIST_PROMPT instructs to use find_cross_references tool and format results"
    - "Validator checks for source attribution ('Source:') in responses that used tools"
    - "Validator checks for scannable format (warns on prose-heavy responses without line breaks)"
    - "max_output_tokens is 2048 (up from 1024) to avoid truncation with attribution + cross-refs"
  artifacts:
    - path: "backend/src/agent/prompts.py"
      provides: "Enhanced SPECIALIST_PROMPT with response format template"
      contains: "RESPONSE FORMAT"
    - path: "backend/src/agent/validator.py"
      provides: "Source attribution and formatting validators"
      contains: "source attribution"
    - path: "backend/src/agent/config.py"
      provides: "Increased max_output_tokens"
      contains: "max_output_tokens=2048"
    - path: "backend/tests/test_agent.py"
      provides: "Updated prompt and validator unit tests"
      contains: "source_attribution"
  key_links:
    - from: "backend/src/agent/prompts.py"
      to: "find_cross_references tool"
      via: "prompt instruction to call tool"
      pattern: "find_cross_references"
    - from: "backend/src/agent/validator.py"
      to: "API layer (app.py)"
      via: "validate_response called after ainvoke"
      pattern: "source.*attribution"
---

<objective>
Rewrite the Specialist prompt's RESPONSE FORMAT section with explicit structured templates for source attribution and cross-references, expand the deterministic validator with new quality checks, and increase max_output_tokens to prevent truncation.

Purpose: Satisfies RESP-01 (source attribution), RESP-05 (clean scannable format), and supports RESP-04 (cross-reference display). The Specialist currently has a 4-line response format section -- this replaces it with an explicit template that Gemini 2.5 Flash will follow reliably.

Output: Enhanced prompt, expanded validator, bumped token limit, and updated unit tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-response-quality/08-RESEARCH.md
@backend/src/agent/prompts.py
@backend/src/agent/validator.py
@backend/src/agent/config.py
@backend/tests/test_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SPECIALIST_PROMPT response format and bump max_output_tokens</name>
  <files>backend/src/agent/prompts.py, backend/src/agent/config.py</files>
  <action>
**prompts.py changes:**

Replace the existing `## RESPONSE FORMAT` section (the last 5 lines of SPECIALIST_PROMPT, starting at "## RESPONSE FORMAT") with this expanded section. Keep everything above it unchanged (CRITICAL RULES, NOT-AVAILABLE FIELD HANDLING, OUT-OF-SCOPE HANDLING). Also add `find_cross_references` to the tool instructions.

Changes to make:

1. In the CRITICAL RULES section, add rule 6:
```
6. **ALWAYS include source attribution.** Every response that uses tool data \
MUST end with a "Source:" line citing the document name from source_documents. \
This is non-negotiable.
```

2. Add rule 7:
```
7. **Use find_cross_references** after retrieving spec data to check if the \
same component is used in other models. Include cross-reference info when \
matches are found.
```

3. Replace the `## RESPONSE FORMAT` section entirely with:

```
## RESPONSE FORMAT

CRITICAL: Every response MUST follow this structure. Never skip the Source line.

### Structure
1. **Direct Answer** -- Lead with the specific value (part number, HP, wattage, count)
2. **Details** -- Bulleted list of specs, NOT prose paragraphs
3. **Cross-References** -- If find_cross_references returns matches, include them
4. **Source** -- ALWAYS end with a Source: line from source_documents

### Formatting Rules
- Use **bold** for part numbers and key numeric values
- Use bulleted lists for multiple specs, never paragraph form
- Keep responses concise: 3-10 lines typical, never more than 15
- When a component is shared across ALL models from the same manufacturer, say \
"This [component] is shared across all [Manufacturer] [Series] models"
- When shared across some models, list them: "Also used in: [Model1], [Model2]"

### Source Attribution
- ALWAYS include at the end: "Source: [document_name], page [page_number]"
- For website sources: "Source: [url]"
- Use the source_documents field from the tool response
- If multiple sources exist, cite the most specific one (one with a section name)
- The page number is approximate -- cite it as provided in the data

### Example
The Sundance Aspen uses two **1.1 HP** jet pumps:
- Pump 1: **1.1 HP** continuous, 1-speed, 56 Frame, 11A max
- Pump 2: **1.1 HP** continuous, 1-speed, 56 Frame, 11A max
- Diverter valves: 2

This pump configuration is also used in: Altamar, Cameo, Optima

Source: 880-series-2026.pdf, page 22
```

**config.py changes:**

Change `max_output_tokens=1024` to `max_output_tokens=2048` in the `create_model()` function. Update the docstring to say "max 2048 output tokens" instead of "max 1024 output tokens".

The token increase prevents truncation when responses include source attribution + cross-references (adds ~50-100 tokens per response). Cost difference is negligible with Flash pricing.
  </action>
  <verify>
Run: `python -c "from backend.src.agent.prompts import SPECIALIST_PROMPT; assert 'find_cross_references' in SPECIALIST_PROMPT; assert 'Source:' in SPECIALIST_PROMPT; assert 'ALWAYS include source attribution' in SPECIALIST_PROMPT; print('Prompt checks passed')"`

Run: `python -c "from backend.src.agent.config import create_model; import os; os.environ['GEMINI_API_KEY']='test'; m = create_model(); assert m.max_output_tokens == 2048; print('Token limit check passed')"`
  </verify>
  <done>SPECIALIST_PROMPT contains explicit response format template with source attribution and cross-reference instructions. max_output_tokens increased to 2048. All existing prompt content (anti-hallucination, not-available, out-of-scope) preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Expand validator with source attribution and formatting checks, update tests</name>
  <files>backend/src/agent/validator.py, backend/tests/test_agent.py</files>
  <action>
**validator.py changes:**

Add two new checks to `validate_response()` inside the `if ai_messages:` block, after the existing "null" check:

1. **Source attribution check:** If tool_messages exist (meaning the agent used tools to look up data), check whether the last AI message content contains a source attribution line. Use `re.search(r"[Ss]ource[s]?:", content)`. If not found, append warning: `"No source attribution in response"`.

2. **Prose density / scannable format check:** If the content length is > 200 characters AND the content has fewer than 3 newline-separated lines (`len(content.strip().split("\\n")) < 3`), append warning: `"Response may not be scannable (few line breaks for length)"`.

Update the module docstring to list 5 checks (add the 2 new ones to the list).

**test_agent.py changes:**

Add these new tests in the validator test section:

1. `test_validate_response_no_source_attribution` -- Create state with tool_messages and an AI message that does NOT contain "Source:". Assert warning contains "source attribution".

2. `test_validate_response_has_source_attribution` -- Create state with tool_messages and an AI message that ends with "Source: 880-series-2026.pdf, page 22". Assert NO "source attribution" warning.

3. `test_validate_response_prose_density_warning` -- Create state with tool_messages and an AI message that is >200 chars but a single line (no newlines). Assert warning contains "scannable".

4. `test_validate_response_scannable_format_no_warning` -- Create state with tool_messages and a multi-line AI message (>200 chars with >3 lines using bullet points). Assert NO "scannable" warning.

5. `test_validate_clean_with_source` -- Update the existing `test_validate_response_clean` to include "Source:" in the AI message so it also passes the new source attribution check. The existing clean test's AI message does NOT have source attribution, so it would now produce a warning. Fix: append "\n\nSource: 880-series-2026.pdf, page 22" to the AI message content.

6. `test_specialist_prompt_contains_source_attribution_rule` -- Assert "ALWAYS include source attribution" in SPECIALIST_PROMPT.

7. `test_specialist_prompt_contains_find_cross_references` -- Assert "find_cross_references" in SPECIALIST_PROMPT.

8. `test_specialist_prompt_contains_response_format_template` -- Assert "Direct Answer" in SPECIALIST_PROMPT and "Formatting Rules" in SPECIALIST_PROMPT.

9. `test_create_model_max_output_tokens` -- Assert create_model().max_output_tokens == 2048 (update from implicit 1024 check if one exists, or add new).
  </action>
  <verify>
Run: `cd /c/dvl/IA/spaparts/dex/backend && python -m pytest tests/test_agent.py -v --timeout=30`

All existing + new tests pass. No regression on prior validator or prompt tests.

Count: `python -m pytest tests/test_agent.py --co -q` -- should show total test count increased by ~9 tests.
  </verify>
  <done>Validator has 5 checks (original 3 + source attribution + prose density). All test_agent.py tests pass including 9+ new tests for validator, prompt, and config changes. Existing clean test updated to pass with new source attribution check.</done>
</task>

</tasks>

<verification>
1. `cd /c/dvl/IA/spaparts/dex/backend && python -m pytest tests/test_agent.py -v --timeout=30` -- all tests pass
2. `python -c "from backend.src.agent.prompts import SPECIALIST_PROMPT; assert 'Source:' in SPECIALIST_PROMPT and 'find_cross_references' in SPECIALIST_PROMPT and 'Direct Answer' in SPECIALIST_PROMPT; print('OK')"` -- prompt has all required sections
3. `python -c "from backend.src.agent.validator import validate_response; w = validate_response({'messages': [type('M', (), {'type': 'tool', 'content': '{}'})(), type('M', (), {'type': 'ai', 'content': 'The pump is 2.5 HP.'})()]}); assert 'source attribution' in str(w).lower(); print('Validator catches missing source')"` -- validator detects missing source
4. `python -c "from backend.src.agent.config import create_model; import os; os.environ['GEMINI_API_KEY']='test'; assert create_model().max_output_tokens == 2048; print('OK')"` -- token limit correct
</verification>

<success_criteria>
- SPECIALIST_PROMPT RESPONSE FORMAT section rewritten with Direct Answer / Details / Cross-References / Source structure
- SPECIALIST_PROMPT includes rules 6 (source attribution) and 7 (find_cross_references)
- SPECIALIST_PROMPT includes explicit example response with source line
- Validator checks for source attribution when tool messages present
- Validator checks for scannable format (prose density)
- max_output_tokens increased from 1024 to 2048
- 9+ new unit tests all passing
- All existing tests pass (no regression, with clean test updated for new source check)
</success_criteria>

<output>
After completion, create `.planning/phases/08-response-quality/08-02-SUMMARY.md`
</output>
